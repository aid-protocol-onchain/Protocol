# ───────────────────────── secrets, NEVER commit ─────────────────────────
.env
.env.*
!.env.example
.dev.vars
.cloudflare.env
.admin-token
*.admin-token
.mcp.json
# deployer / signer keypairs (Solana + EVM)
*-deployer*.json
.devnet-deployer.json
chain/solana/.devnet-deployer.json
**/id.json
**/*keypair*.json
*.pem
*.key

# ───────────────────────── dependencies / builds ─────────────────────────
node_modules/
dist/
build/
.wrangler/
.astro/
*.tsbuildinfo

# Rust / Anchor / Solana
target/
.anchor/
test-ledger/

# Foundry / EVM
out/
cache/
chain/evm/lib/
chain/evm/broadcast/
chain/evm/cache/
lcov.info

# ───────────────────────── local tooling (not product) ─────────────────────────
.claude/*
!.claude/skills/
.claude/skills/*
# Tracked: the project's Solana skill + custom BMad agents (rest of .claude stays local)
!.claude/skills/solana-dev/
!.claude/skills/bmad-agent-solana-program/
!.claude/skills/bmad-agent-solana-client/
!.claude/skills/bmad-agent-solana-auditor/
!.claude/skills/bmad-agent-evm-auditor/
!.claude/skills/solidity-auditor/
_bmad/
{output_folder}/

# ───────────────────────── dev artifacts / QA screenshots ─────────────────────────
.playwright-mcp/
# root-level screenshots from local QA (real assets live under app/public, app/src/assets)
/*.png
STATUS.md

# ───────────────────────── editor / OS ─────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# HyperFrames + skills CLI install artifacts (the tracked skills live under .claude/skills/)
.agents/
skills-lock.json
agent/
# HyperFrames video working project (rendered output is hosted + committed under app/coming-soon/public/)
videos/
